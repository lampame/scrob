name: Release

on:
  push:
    branches: [main]

jobs:
  release:
    if: github.repository == 'lampame/scrob'
    name: Semantic release
    runs-on: ubuntu-latest
    permissions:
      contents: write
      issues: write
      pull-requests: write

    outputs:
      new_release_published: ${{ steps.release.outputs.new_release_published }}
      new_release_version: ${{ steps.release.outputs.new_release_version }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Run semantic-release
        id: release
        uses: cycjimmy/semantic-release-action@v4
        with:
          extra_plugins: |
            conventional-changelog-conventionalcommits@8
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  docker:
    name: Build & push release images
    needs: release
    if: needs.release.outputs.new_release_published == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: true
          build-args: |
            APP_VERSION=${{ needs.release.outputs.new_release_version }}
          tags: |
            lampame/scrob:latest
            lampame/scrob:main
            lampame/scrob:${{ needs.release.outputs.new_release_version }}
            ghcr.io/lampame/scrob:latest
            ghcr.io/lampame/scrob:main
            ghcr.io/lampame/scrob:${{ needs.release.outputs.new_release_version }}
          cache-from: type=gha,scope=scrob-release
          cache-to: type=gha,mode=max,scope=scrob-release

      - name: Build and push omnibus image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile.omnibus
          platforms: linux/amd64,linux/arm64
          push: true
          build-args: |
            APP_VERSION=${{ needs.release.outputs.new_release_version }}
          tags: |
            lampame/scrob:latest-omnibus
            lampame/scrob:main-omnibus
            lampame/scrob:${{ needs.release.outputs.new_release_version }}-omnibus
            ghcr.io/lampame/scrob:latest-omnibus
            ghcr.io/lampame/scrob:main-omnibus
            ghcr.io/lampame/scrob:${{ needs.release.outputs.new_release_version }}-omnibus
          cache-from: type=gha,scope=scrob-omnibus-release
          cache-to: type=gha,mode=max,scope=scrob-omnibus-release
